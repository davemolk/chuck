These are self-signed, throwaway TLS certificates for local development only.

They are included to minimize setup steps for reviewers and should never be used or committed in a real production environment.
